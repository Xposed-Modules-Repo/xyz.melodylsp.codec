<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="false"
        android:label="@string/app_label"
        android:description="@string/app_description"
        android:icon="@mipmap/ic_launcher"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!--
          libxposed API 101+ reads scope from META-INF/xposed/scope.list and module
          metadata from META-INF/xposed/module.prop. The legacy
          xposedmodule / xposedminversion / xposedscope manifest metadata are no
          longer used by API 101 frameworks.
        -->

        <activity
            android:name=".ui.MasterSwitchActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".diag.DiagnosticEventReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="xyz.melodylsp.codec.action.DIAGNOSTIC_EVENT" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
